[build-system]
requires = ["scikit-build-core", 
"wheel", 
"numpy>=1.23"]
build-backend = "scikit_build_core.build"

[project]
name = "pynverse"
version = "0.0.1"
readme = "README.md"
dependencies = [
    "numpy>=1.23"
]